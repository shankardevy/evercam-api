<div class='col-sm-10 col-sm-offset-1'>
  <h1>Almost Done</h1>
  <p>Welcome back <%= @user.firstname %>, you're almost finished. Now that we've confirmed your email address the last step is to create a new password for your account. The stronger the better and remember, it's more secure to use a unique password for every site you register on.</p>
  <form method="POST" role="form">

    <div class="form-group">
      <div class="form-inline">
        <div class="form-group col-xs-6">
          <label for="password">Password</label>
          <%= input(:password, :password, :required, placeholder: 'Password') %>
        </div>
        <div class="form-group col-xs-6">
          <label for="confirmation">Confirmation</label>
          <%= input(:confirmation, :password, :required, placeholder: 'Confirmation') %>
        </div>
      </div>
    </div>

    <input class="btn btn-primary pull-right" type="submit" value="Finish"></input>

  </form>
</div>

